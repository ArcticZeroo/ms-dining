model RuntimeMigration {
  name        String    @id
  status      String    @default("pending")
  completedAt DateTime?
  error       String?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
}
